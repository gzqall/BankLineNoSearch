<?xml version="1.0" encoding="UTF-8"?>

<configuration scan="true" scanPeriod="30 seconds">
	<appender name="callbackAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${catalina.base}/logs/banklinesearch/banklinesearch.log</file>
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${catalina.base}/logs/callback/callback.%d{yyyy-MM-dd}.log</fileNamePattern>
			<maxHistory>30</maxHistory>
		</rollingPolicy>
		<layout class="ch.qos.logback.classic.PatternLayout">
			<Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{40} - %msg%n</Pattern>
		</layout>
	</appender>
	<!--控制台-->
	<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{40} - %msg%n</pattern>
			<charset>GBK</charset>
		</encoder>
	</appender>
	<!--*************************【打印日志】**************************-->
	<!-- 单笔代收日志 -->
	<logger name="banklinesearchLogger" additivity="false">
		<level value="info" />
		<appender-ref ref="callbackAppender" />
	</logger>
	<root level="INFO">
		<appender-ref ref="console" />
	</root>
</configuration>